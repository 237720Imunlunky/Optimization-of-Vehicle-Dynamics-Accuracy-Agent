请在该文件夹下进行本次对话

一、文件现有内容介绍：
1.初版参数配置文件夹中：文件夹内提供车辆初始动力学参数配置文件，本方案仅针对Carsim车辆动力学本体参数进行迭代优化，不修改Carsim底层物理模型、不改动工况脚本与控制逻辑，该类动力学参数为全程优化目标变量。
2.各工况条件、评价指标及评价方法文件夹：
（1）纵向动力学工况、评价指标：目前纵向动力学的工况包括两个试验一个是滑行试验、一个是加速试验，每张图片中均提供响应的工况描述与评价指标
（2）单工况精度计算方法：每个工况中的评价指标一般包含时域拟合类指标与特征精度类指标，文件中提供这两种指标的计算公式
单工况中各评价指标计算后的数值需满足其具体要求，然后再根据单工况聚合精度提供的权重进行加权计算，来得到单工况精度。
（3）各方向动力学工况权重：纵向动力学精度计算方式md文档中包含了纵向动力学工况的权重系数请根据（2）中计算得到的单工况精度在进行加权计算来得到纵向动力学整体精度，项目验收阈值为：纵向动力学精度≥80%
3.实车数据文件夹：提供滑行、两类加速工况对应的实车路测Trace数据及配套DBC解码文件，可解析得到实车真值数据与驾驶员操作信号（方向盘、油门、制动等控制输入）。
实车数据中包含驾驶员的操作信号：可将解析后的真实驾驶员控制时序信号，替代Carsim原生工况脚本控制输入，实现仿真输入与实车输入完全对齐，仅保留车辆动力学参数为唯一变量，保证参数标定的唯一性与有效性。

二、前置仿真工况搭建
本方案无需手动搭建Carsim原生标准工况脚本，直接采用实车Trace解码后的真实驾驶员操作信号（油门开度、制动状态、行驶操作时序等）替代Carsim原有工况控制输入。仅统一对齐实车试验对应的初始车速、路面条件、仿真时长等固定边界，让仿真控制输入与实车试验输入完全一致，全程仅保留车辆动力学参数为唯一变量，彻底消除工况输入差异带来的对标误差，为LLM闭环参数迭代优化提供高精度、可溯源的标准化仿真对标环境。

三、LLM动力学参数优化Agent整体流程
本系统通过API接入大模型（包含API Key、请求地址、模型名称等完整部署参数），构建闭环迭代优化Agent，LLM仅作为参数智能迭代优化器，不替代Carsim物理动力学模型，整体迭代流程如下：
1. 初始仿真迭代：加载初始车辆动力学参数，在搭建完成的Carsim标准工况中批量仿真，输出各工况仿真响应数据。
2. 数据对标与精度计算：将仿真数据与DBC解码后的实车真值数据对标，按照既定的时域、特征值指标公式完成全部单项指标计算，逐级聚合得到单工况精度、纵向动力学整体精度。
3. 精度判停逻辑：判断纵向动力学整体精度是否满足≥85%的验收要求。若满足精度阈值，直接输出当前最优车辆动力学参数，终止迭代流程。
4. LLM智能参数迭代更新：若精度不达标，Agent将当前所有工况偏差信息、指标得分、参数物理约束输入LLM，由LLM基于动力学机理与偏差归因，智能调整、更新车辆动力学参数，输出合规的新参数集。
5. 闭环迭代优化：加载LLM更新后的参数重新进入Carsim仿真，重复“仿真输出—精度评价—LLM参数优化”闭环流程，直至整体精度满足验收标准。
ps：参数优化请注意参数对工况输出数据影响之间的耦合关系

四、核心重点说明
本方案核心聚焦LLM对车辆动力学参数的智能迭代优化能力，全程依托Carsim原生物理模型保证仿真可信度，依托实车真值与标准化评价体系保证标定精度，通过大模型实现传统人工调参的自动化、智能化迭代，最终实现仿真车辆纵向动力学特性高精度拟合实车状态。

补充说明：由于当前实车部门可提供的实车测试工况数据有限，所以当前仅进行纵向动力学方向的数据精度计算和响应的参数优化，后续随着实车提供其他工况条件下的数据，我们会再对Agent进行完善。